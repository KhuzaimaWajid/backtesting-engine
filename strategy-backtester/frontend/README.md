# Frontend

React + TypeScript (Vite) UI for the Backtest Engine. See the [root README](../README.md)
for setup, the API reference, and project-wide documentation.

Quick start:

```bash
npm install
cp .env.example .env.local   # set VITE_API_BASE_URL if the backend isn't on :8000
npm run dev
```
